{
  "version": "0.0.1",
  "name":"babel-plugin-log-consolations",
  "description": "Tells you where you called console.log from",
  "repository": "aware/babel-plugin-log-consolations",
  "author": "alex ware <alex.ware@guardian.co.uk>",
  "main": "lib/index.js",
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "jest": "^21.1.0",
    "standard": "^10.0.3"
  },
  "scripts": {
    "clean": "rm -rf lib",
    "build": "babel src -d lib",
    "test": "standard && jest",
    "lint":"standard --fix",
    "prepublish": "npm run clean && npm run build"
  },
  "keywords": [
    "babel-plugin"
  ]
}
